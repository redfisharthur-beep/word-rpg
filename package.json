{
  "name": "word-rpg",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:data": "node scripts/sync-game-data.mjs",
    "dev": "npm run sync:data && wrangler dev",
    "deploy": "npm run sync:data && wrangler deploy",
    "test": "npm run sync:data && node tests/rpg-systems.mjs && node tests/words.mjs && node tests/v2-systems.mjs && node tests/pk-render.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}
